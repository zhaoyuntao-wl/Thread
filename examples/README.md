# Thread Examples

Minimal runnable examples for the Thread core API.

## quick-start.ts

Session memory in ~40 lines: open the store, append events, goal detection, an
explicit-channel decision, the status card, and on-demand retrieval.

```sh
pnpm install
npx tsx examples/quick-start.ts
```

The example uses a temp directory; nothing is written outside it.
