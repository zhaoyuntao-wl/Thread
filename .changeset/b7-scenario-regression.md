---
"@thread/evals": minor
---

B⑦ 场景级保真回归集：新增 compact-fidelity（跨压缩保真）/ injection-follow（注入遵循前置）/ scope-filter（作用域隔离）/ migration-lossless（迁移无损）/ rebuild-recovery（删结构化库后事件流水重放恢复）五场景，harness 支持 compact/status-card 检查，新增 `pnpm eval` 门禁（eval-cli 聚合全部场景、非零退出码）。
