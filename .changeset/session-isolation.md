---
"@thread/core": minor
---

会话临时隔离（B⑧）：会话内可变开关（自然语言"隔离/静默/解除隔离" + `/isolate` `/unisolate` `/thread-publish <kind> <id>` 命令）——对话上下文（消息/决策/反馈）全链路仅自己可见（合并视图 / search / queryEvents / expand / 血缘过滤），项目事实（tool 事件）共享不断链；解除后历史仍隔离、按需沉淀转共享。core schema v3（isolation 列 + session_isolation 表）；dsh-thread 与 Qoder hooks 双端接入；evals 新增 isolation 场景（门禁 10/10）。
