---
"@thread/core": minor
"@thread/adapter-qoder-cli": minor
---

B③ 跨会话自动继承（轻量版）：合并视图新增分层优先级裁决 `applyScopePriority`——同事实（归一化 text）跨层级出现时保留最高优先级（会话内 > 项目 > 全局），项目特例覆盖全局默认；状态卡继承展示接入裁决（同项目历史会话 active 决策 / 全局反馈开场即见，机制词汇不进状态卡）；回归集新增跨会话继承场景（新会话继承旧会话决策与反馈、优先级去重、默认 project 级成为后续会话继承源）。
