---
"dsh-thread": minor
"thread-mcp": minor
"t-dsh": minor
---

发布件三件套结构就绪（发布门槛② 一键可装）：thread-mcp 独立 MCP server 包（bin + better-sqlite3 随包解决 bindings）；dsh-thread 发布名 + better-sqlite3 依赖；t-dsh 一键 profile（bin 安装脚本生成 ~/.dsh/profiles/thread/）。CI 加 dsh-thread compat 矩阵（发布门槛③，钉 0.1.0-rc.6）。
