---
"@thread/core": minor
"@thread/adapter-qoder-cli": minor
---

结构化行治理可见性（B⑥-② 恢复通道配套①②）：① 状态卡目标/决策/偏好行尾显示行 id（`#12`）——写入即见、每轮常驻，`/feedback-del` `/thread-publish` 可定位；② MCP `query_session_memory` 的 `kind` 扩展 `goal/decision/feedback`，路由到新增 `queryStructured`（结构化表直查，隔离语义与事件一致）——忘掉 id 后的后置查询入口。附单测 5 例（queryStructured 3 + 行 id 1 + 恢复通道既有）。
