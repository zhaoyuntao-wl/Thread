{
  "name": "thread-mcp",
  "version": "0.0.0",
  "description": "Thread session memory MCP server — query_session_memory tool for any MCP-capable host (dsh --patch overlay, Claude Code, etc.)",
  "license": "MIT",
  "type": "module",
  "bin": {
    "thread-mcp": "./dist/server.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@thread/core": "workspace:*",
    "better-sqlite3": "^12.0.0",
    "zod": "^4.4.3"
  }
}
